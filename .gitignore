node_modules/
dist/
.pi-subagents/

# Local copies of offline basemap archives (~283MB each) — hosted on
# Cloudflare R2, not committed to the repo. See docs/OFFLINE_TILES.md.
app/public/tiles/*.pmtiles

{
  "name": "free-camping-app",
  "private": true,
  "version": "0.1.0",
  "description": "Offline-capable map app for the free dispersed camping dataset",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "gen-icons": "node scripts/gen-icons.mjs",
    "predev": "node scripts/sync-data.mjs",
    "dev": "vite",
    "prebuild": "node scripts/sync-data.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/filters.test.mjs"
  },
  "dependencies": {
    "maplibre-gl": "^6.1.0",
    "pmtiles": "^4.4.1"
  },
  "devDependencies": {
    "vite": "^8.2.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
